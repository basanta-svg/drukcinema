{
  "name": "drukcinema",
  "version": "1.0.0",
  "description": "DrukCinema — Bhutan's premier movie booking platform",
  "private": true,
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "start cmd /k \"cd client && npm run dev\" && start cmd /k \"cd server && npm run dev\"",
    "install:all": "cd client && npm install && cd ../server && npm install",
    "seed": "cd server && node seed.js"
  }
}
